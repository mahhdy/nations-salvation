
% ─── بسته‌های پایه ───────────────────────────────────────────────────────
\usepackage{fontspec}
\usepackage{geometry}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{makecell}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{tcolorbox}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{titletoc}
\usepackage{hyperref}
\usepackage{bookmark}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{adjustbox}
\usepackage{rotating}
\usepackage{setspace}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{footnote}
\usepackage{etoolbox}
\usepackage{calc}
\usepackage{needspace}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{pdfpages}
\usepackage{pdflscape}
\usepackage{float}
\usepackage{ragged2e}

% ─── تنظیمات TikZ ───────────────────────────────────────────────────────
\usetikzlibrary{
  arrows, arrows.meta, positioning, shapes, shapes.geometric,
  shapes.multipart, shadows, decorations, decorations.pathreplacing,
  decorations.markings, patterns, backgrounds, calc, fit,
  mindmap, trees, matrix, chains, scopes,
  fadings, plotmarks
}
\pgfplotsset{compat=1.18}
\tcbuselibrary{skins, breakable}

% ─── مسیر تصاویر ────────────────────────────────────────────────────────
\graphicspath{{images/}{figures/}}

% ─── صفحه‌آرایی ─────────────────────────────────────────────────────────
\geometry{
  a4paper,
  top=2.8cm, bottom=2.8cm,
  right=3.5cm, left=3cm,
  headheight=1.5cm,
  headsep=0.5cm,
  footskip=1.2cm
}

% ─── پالت رنگی ──────────────────────────────────────────────────────────
\definecolor{PrimaryDeep}{HTML}{1A237E}
\definecolor{PrimaryMid}{HTML}{283593}
\definecolor{PrimaryLight}{HTML}{3949AB}
\definecolor{AccentGold}{HTML}{F9A825}
\definecolor{AccentAmber}{HTML}{FF8F00}
\definecolor{AccentTeal}{HTML}{00695C}
\definecolor{AccentRed}{HTML}{B71C1C}
\definecolor{AccentGreen}{HTML}{1B5E20}
\definecolor{AccentPurple}{HTML}{4A148C}
\definecolor{NeutralLight}{HTML}{ECEFF1}
\definecolor{NeutralMid}{HTML}{B0BEC5}
\definecolor{NeutralDark}{HTML}{455A64}
\definecolor{TextMain}{HTML}{212121}
\definecolor{TextSecond}{HTML}{424242}
\definecolor{BgWarm}{HTML}{FFF8E1}
\definecolor{BgCool}{HTML}{E8EAF6}
\definecolor{BgTeal}{HTML}{E0F2F1}
\definecolor{BgLegal}{HTML}{E3F2FD}
\definecolor{BgSuccess}{HTML}{E8F5E9}
\definecolor{BgFail}{HTML}{FFEBEE}

% رنگ‌های دوره‌ها
\definecolor{EraAncient}{HTML}{4E342E}
\definecolor{EraMedieval}{HTML}{37474F}
\definecolor{EraEarlyMod}{HTML}{1565C0}
\definecolor{EraModern}{HTML}{2E7D32}
\definecolor{EraContemp}{HTML}{6A1B9A}
\definecolor{EraPostmod}{HTML}{AD1457}

% ─── جعبه‌های محتوایی ───────────────────────────────────────────────────
\tcbset{
  mybox/.style={
    enhanced, breakable,
    colback=BgCool, colframe=PrimaryMid,
    fonttitle=\bfseries\large,
    attach boxed title to top right={yshift=-2mm, xshift=-3mm},
    boxed title style={colback=PrimaryMid, colframe=PrimaryDeep},
    top=4mm, bottom=4mm,
    before skip=12pt, after skip=12pt,
  },
  defbox/.style={
    enhanced, breakable,
    colback=BgWarm, colframe=AccentGold,
    fonttitle=\bfseries,
    left=4mm, right=4mm,
    borderline west={3pt}{0pt}{AccentAmber},
    sharp corners,
    before skip=12pt, after skip=12pt,
  },
  wavebox/.style={
    enhanced, breakable,
    colback=BgTeal, colframe=AccentTeal,
    fonttitle=\bfseries,
    rounded corners,
    drop shadow,
    before skip=12pt, after skip=12pt,
  },
  quotebox/.style={
    enhanced, breakable,
    colback=NeutralLight, colframe=NeutralDark,
    fontupper=\itshape,
    left=8mm,
    borderline west={4pt}{0pt}{NeutralMid},
    sharp corners=south,
    before skip=12pt, after skip=12pt,
  },
  enemybox/.style={
    enhanced, breakable,
    colback=BgFail, colframe=AccentRed,
    fonttitle=\bfseries,
    borderline west={3pt}{0pt}{AccentRed},
    before skip=12pt, after skip=12pt,
  },
  casebox/.style={
    enhanced, breakable,
    colback=BgCool!50, colframe=PrimaryLight,
    fonttitle=\bfseries\large,
    left=4mm, right=4mm,
    before skip=12pt, after skip=12pt,
  },
  legalbox/.style={
    enhanced, breakable,
    colback=BgLegal, colframe=PrimaryDeep,
    fonttitle=\bfseries,
    borderline west={3pt}{0pt}{PrimaryDeep},
    left=4mm, right=4mm,
    before skip=12pt, after skip=12pt,
  },
  successbox/.style={
    enhanced, breakable,
    colback=BgSuccess, colframe=AccentGreen,
    fonttitle=\bfseries,
    borderline west={3pt}{0pt}{AccentGreen},
    before skip=12pt, after skip=12pt,
  },
  failbox/.style={
    enhanced, breakable,
    colback=BgFail, colframe=AccentRed,
    fonttitle=\bfseries,
    borderline west={3pt}{0pt}{AccentRed},
    before skip=12pt, after skip=12pt,
  },
  timelinebox/.style={
    enhanced, breakable,
    colback=white, colframe=NeutralDark,
    fonttitle=\bfseries\large,
    left=2mm, right=2mm,
    sharp corners,
    boxrule=0.4pt,
    before skip=12pt, after skip=12pt,
  },
}

% ─── سبک‌های TikZ سفارشی ────────────────────────────────────────────────
\tikzset{
  timeline arrow/.style={
    ->,
    >={Stealth[length=6pt]},
    line width=2pt,
    color=PrimaryMid,
  },
  event node/.style={
    draw=PrimaryLight,
    fill=BgCool,
    rounded corners=3pt,
    text width=3.8cm,
    align=center,
    font=\small,
    inner sep=4pt,
  },
  era label/.style={
    font=\bfseries\small,
    text=white,
    rounded corners=2pt,
    inner sep=4pt,
  },
  success mark/.style={
    circle, fill=AccentGreen, text=white,
    font=\bfseries\small, inner sep=2pt,
  },
  fail mark/.style={
    circle, fill=AccentRed, text=white,
    font=\bfseries\small, inner sep=2pt,
  },
  mixed mark/.style={
    circle, fill=AccentAmber, text=white,
    font=\bfseries\small, inner sep=2pt,
  },
}

% ─── سربرگ و پابرگ ──────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0.6pt}
\renewcommand{\footrulewidth}{0.3pt}
\fancyhead[RO,LE]{\small\color{PrimaryMid}\rightmark}
\fancyhead[LO,RE]{\small\color{NeutralDark}ملت‌ها و نجات‌بخشی \textbar{} مهدی سالم}
\fancyfoot[C]{\small\thepage}

\fancypagestyle{plain}{%
  \fancyhf{}%
  \renewcommand{\headrulewidth}{0pt}%
  \renewcommand{\footrulewidth}{0.3pt}%
  \fancyfoot[C]{\small\thepage}%
}

% ─── عنوان‌بندی بخش‌ها ──────────────────────────────────────────────────

% فرمت part
\titleformat{\part}[display]
  {\centering\Huge\bfseries\color{PrimaryDeep}}
  {\large\color{AccentGold}\partname\ \thepart}
  {12pt}
  {\Huge\color{PrimaryDeep}}
\titlespacing*{\part}{0pt}{60pt}{40pt}

% فرمت chapter
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{PrimaryDeep}}
  {\large\color{AccentGold}\chaptertitlename\ \thechapter}
  {12pt}
  {\Huge}
\titlespacing*{\chapter}{0pt}{40pt}{30pt}

% فرمت section
\titleformat{\section}[block]
  {\needspace{6\baselineskip}\Large\bfseries\color{PrimaryDeep}}
  {\thesection}{1em}{}
  [{\color{AccentGold}\titlerule[1.5pt]}]
\titlespacing*{\section}{0pt}{18pt}{10pt}

% فرمت subsection
\titleformat{\subsection}[block]
  {\needspace{4\baselineskip}\large\bfseries\color{PrimaryMid}}
  {\thesubsection}{0.8em}{}
  [{\color{NeutralMid}\titlerule[0.6pt]}]
\titlespacing*{\subsection}{0pt}{14pt}{6pt}

% فرمت subsubsection
\titleformat{\subsubsection}[runin]
  {\normalsize\bfseries\color{AccentTeal}}
  {\thesubsubsection}{0.6em}{}[\ —\ ]
\titlespacing*{\subsubsection}{0pt}{10pt}{4pt}

% ─── فاصله‌گذاری ─────────────────────────────────────────────────────────
\setlength{\parindent}{1em}
\setlength{\parskip}{0.4em}
\onehalfspacing

% ─── hyperref ─────────────────────────────────────────────────────────────
\hypersetup{
  colorlinks  = true,
  linkcolor   = PrimaryMid,
  citecolor   = AccentTeal,
  urlcolor    = AccentAmber,
  pdftitle    = {ملت‌ها و نجات‌بخشی},
  pdfauthor   = {مهدی سالم},
  pdfsubject  = {فلسفه‌ی سیاسی تطبیقی},
  pdfkeywords = {مداخله‌ی خارجی، نجات‌طلبی، حقوق بین‌الملل، تاریخ ایران},
  bookmarks   = true,
  pdfpagemode = UseOutlines,
  pdfencoding = unicode,
}